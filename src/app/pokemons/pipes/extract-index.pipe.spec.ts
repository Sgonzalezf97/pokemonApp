import { ExtractIndexPipe } from './extract-index.pipe';

describe('ExtractIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
